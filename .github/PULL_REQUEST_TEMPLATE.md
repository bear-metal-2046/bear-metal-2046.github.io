## Description

What changed? Why? (the code + comments should speak for itself on the "how")

Any issues this pull request closes or pull requests this supersedes should be linked with `Closes #issue_number`.

## Checklist

- [ ] Lint and spell check pass locally (`pnpm test`)
- [ ] Updated `nav:` in `mkdocs.yml` if a new page was added
- [ ] Branch is up to date with `main`